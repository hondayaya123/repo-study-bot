# reports

This directory contains auto-generated repository learning guides produced by the
[Generate Repo Learning Guide](.github/workflows/generate-learning-guide.yml) workflow.

Each report file follows the naming convention:

```
{owner}_{repo}_{level}_{goal}_{timestamp}.md
```

Reports are committed here automatically after each successful workflow run.
